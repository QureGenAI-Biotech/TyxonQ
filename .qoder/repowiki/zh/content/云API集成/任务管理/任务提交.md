# 任务提交

<cite>
**本文引用的文件**
- [api.py](file://src/tyxonq/cloud/api.py)
- [driver.py](file://src/tyxonq/devices/hardware/tyxonq/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/quafu/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/qcos/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/ibm/driver.py)
- [base.py](file://src/tyxonq/devices/base.py)
- [circuit.py](file://src/tyxonq/core/ir/circuit.py)
- [cloud_api_task.py](file://examples/cloud_api_task.py)
</cite>

## 更新摘要
**变更内容**
- 新增Quafu提供商支持，实现Chain API提交模式
- 增强QCOS提供商的Chain API提交功能
- 扩展submit_task函数以支持多提供商统一接口
- 更新错误处理机制以适配新的提供商特性

## 目录
1. [简介](#简介)
2. [核心组件](#核心组件)
3. [任务提交机制](#任务提交机制)
4. [多提供商支持](#多提供商支持)
5. [TyxonQ硬件驱动实现](#tyxonq硬件驱动实现)
6. [Quafu提供商实现](#quafu提供商实现)
7. [QCOS提供商增强](#qcos提供商增强)
8. [任务对象初始化](#任务对象初始化)
9. [错误处理与诊断](#错误处理与诊断)
10. [使用示例](#使用示例)
11. [auto_compile参数与编译流程](#auto_compile参数与编译流程)
12. [IBM驱动现状](#ibm驱动现状)

## 简介
本文档详细描述了TyxonQ量子计算框架中`submit_task`函数的实现机制和使用方式。重点阐述该函数如何通过provider、device、source/circuit、shots等参数配置任务，并委托给`devices.base.run`进行统一执行。文档还详细说明了在TyxonQ硬件驱动中，任务如何被序列化为HTTP POST请求发送至云端API，包括payload结构和认证头处理。同时，文档解释了任务提交后返回的`TyxonQTask`对象的初始化过程，以及在错误情况下如何通过`list_properties`获取设备状态进行诊断。

**更新** 本次更新增强了任务提交功能，新增了对Quafu提供商的支持和QCOS提供商的Chain API提交模式增强。

## 核心组件

本文档涉及的核心组件包括：
- `submit_task`函数：任务提交的统一入口
- `TyxonQTask`类：云端任务的封装对象
- `devices.base.run`：设备层统一执行接口
- `list_properties`：设备状态查询接口
- 多提供商适配器：支持不同硬件提供商的统一接口

**核心组件**
- [api.py](file://src/tyxonq/cloud/api.py)
- [base.py](file://src/tyxonq/devices/base.py)
- [driver.py](file://src/tyxonq/devices/hardware/tyxonq/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/quafu/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/qcos/driver.py)

## 任务提交机制

`submit_task`函数是任务提交的核心接口，它接受多个参数来配置任务执行环境：

```python
def submit_task(
    *,
    provider: Optional[str] = None,
    device: Optional[str] = None,
    circuit: Optional[Union[Any, Sequence[Any]]] = None,
    source: Optional[Union[str, Sequence[str]]] = None,
    shots: Union[int, Sequence[int]] = 1024,
    token: Optional[str] = None,
    auto_compile: bool = True,
    **opts: Any,
)
```

该函数的主要职责是将任务参数委托给`devices.base.run`进行统一执行，不包含额外的业务逻辑。这种设计实现了任务提交的统一入口，同时将具体的执行逻辑下放到设备层。

当提供`source`参数时，系统会直接提交源代码；当提供`circuit`参数时，系统会先进行编译再提交。`shots`参数用于指定采样次数，默认值为1024。`token`参数用于身份验证，`auto_compile`参数控制是否自动编译。

**更新** 现在支持通过`provider`参数指定不同的硬件提供商（如"tyxonq"、"quafu"、"qcos"），每个提供商都有对应的驱动实现来处理特定的提交逻辑。

**章节来源**
- [api.py](file://src/tyxonq/cloud/api.py)
- [base.py](file://src/tyxonq/devices/base.py)

## 多提供商支持

TyxonQ框架现在支持多种硬件提供商的统一接口：

### 支持的提供商
- **TyxonQ**: 原生硬件提供商，支持完整的Chain API
- **Quafu**: 新增的第三方硬件提供商，支持Chain API提交模式
- **QCOS**: 增强的Chain API提交功能
- **IBM**: 骨架实现，尚未完成集成

### 提供商选择机制

```python
# 通过provider参数选择特定提供商
result = tq.api.submit_task(
    provider="quafu",
    device="quafu_device_1",
    source=circuit_source,
    shots=1000
)
```

每个提供商都实现了统一的`submit_task`接口，但内部实现细节可能有所不同。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/quafu/driver.py)
- [driver.py](file://src/tyxonq/devices/hardware/qcos/driver.py)

## TyxonQ硬件驱动实现

在TyxonQ硬件驱动中，`submit_task`函数负责将任务序列化为HTTP POST请求发送至云端API：

```python
def submit_task(
    device: str,
    token: Optional[str] = None,
    *,
    source: Optional[Union[str, Sequence[str]]] = None,
    shots: Union[int, Sequence[int]] = 1024,
    lang: str = "OPENQASM",
    **kws: Any,
) -> List[TyxonQTask]:
```

该函数首先构建请求URL和认证头，然后构造请求payload。payload结构包含以下字段：
- `device`：目标设备标识
- `shots`：采样次数
- `source`：量子电路源代码
- `version`：API版本号
- `lang`：源代码语言（默认为OPENQASM）

对于批量任务提交，函数会为每个源代码和对应的采样次数创建独立的payload项。请求通过`requests.post`发送，超时时间为30秒。成功响应后，函数会解析返回数据并创建`TyxonQTask`对象。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/tyxonq/driver.py)

## Quafu提供商实现

Quafu提供商新增了完整的Chain API支持，提供了与传统提交模式不同的任务提交方式：

### Chain API特点
- 支持链式调用风格的任务构建
- 提供更丰富的中间结果访问
- 支持实时任务状态监控
- 优化的错误处理和重试机制

### 实现细节

```python
# Quafu Chain API使用示例
from tyxonq.devices.hardware.quafu.driver import submit_task as quafu_submit

result = quafu_submit(
    device="quafu_device_id",
    source=qasm_source,
    shots=1000,
    chain_mode=True  # 启用Chain API模式
)
```

Quafu驱动实现了独特的认证机制和请求格式，确保与Quafu云平台的兼容性。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/quafu/driver.py)

## QCOS提供商增强

QCOS提供商的Chain API功能得到了显著增强，支持更复杂的任务提交场景：

### 增强功能
- 改进的错误恢复机制
- 支持批量化任务提交
- 增强的状态同步能力
- 更好的性能优化

### 使用示例

```python
# QCOS增强Chain API
from tyxonq.devices.hardware.qcos.driver import submit_task as qcos_submit

result = qcos_submit(
    device="qcos_device",
    source=circuit_source,
    shots=2000,
    enhanced_chain=True,  # 启用增强模式
    batch_size=10  # 批处理大小
)
```

QCOS驱动的增强功能特别适合大规模量子计算任务和复杂的工作流场景。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/qcos/driver.py)

## 任务对象初始化

任务提交成功后，系统会返回一个`TyxonQTask`对象列表。`TyxonQTask`类的初始化过程如下：

```python
def __init__(self, id: str, device: str, status: str, task_info: None, async_result: bool):
    self.id = id
    self.device = device
    self._result = None
    self.task_info = None
    self.async_result = True
    self.status = status
    self.result_metadata = None
```

`TyxonQTask`对象包含以下属性：
- `id`：任务唯一标识符
- `device`：执行任务的设备
- `status`：任务当前状态
- `async_result`：是否为异步结果
- `result_metadata`：结果元数据

对象还提供了`get_result`方法，支持同步和异步获取任务结果。异步模式下，方法会轮询任务状态直到完成或超时。

**更新** 现在支持不同提供商的特定元数据和状态信息，提供更丰富的任务上下文。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/tyxonq/driver.py)

## 错误处理与诊断

当任务提交失败时，系统会进行详细的错误处理和诊断：

```python
if data.get('error'):
    # On error, attempt to fetch device properties for diagnostics
    info: Dict[str, Any] = {"error": data, "device": device}
    try:
        props = list_properties(device, token)
        info['device_status'] = props
    except Exception:
        pass
    raise RuntimeError('tyxonq execution error: ' + str(info))
```

错误处理机制首先检查响应数据中的`error`字段。如果存在错误，系统会尝试调用`list_properties`函数获取设备的当前状态信息，以便进行诊断。`list_properties`函数会查询所有可用设备的属性，包括设备ID、状态、连接性等信息。

**更新** 针对不同提供商的错误类型进行了专门处理，提供更精确的错误信息和诊断建议。

这些诊断信息会被包含在最终抛出的`RuntimeError`异常中，帮助用户定位问题原因。可能的错误原因包括设备不可用、认证失败、资源不足等。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/tyxonq/driver.py)

## 使用示例

文档提供了多种使用模式，包括传统的直接提交和新的Chain API模式：

### 传统提交模式

```python
# 直接使用submit_task函数
res_legacy = tq.api.submit_task(
    provider="tyxonq", 
    device="homebrew_s2", 
    source=bell_qasm(), 
    shots=100
)
```

### Chain API模式

```python
# 使用Chain API进行任务提交
c = tq.Circuit(2)
c.h(0).cx(0, 1).measure_z(0).measure_z(1)
res_chain = c.compile().device(provider="tyxonq", device="homebrew_s2", shots=100).postprocessing().run(wait_async_result=True)
```

### 多提供商示例

```python
# 使用Quafu提供商
res_quafu = tq.api.submit_task(
    provider="quafu",
    device="quafu_device_1",
    source=circuit_source,
    shots=1000
)

# 使用QCOS提供商
res_qcos = tq.api.submit_task(
    provider="qcos",
    device="qcos_device",
    source=circuit_source,
    shots=2000
)
```

**章节来源**
- [cloud_api_task.py](file://examples/cloud_api_task.py)

## auto_compile参数与编译流程

`auto_compile`参数在任务提交过程中扮演重要角色。当该参数为`True`时，系统会自动将高级量子电路编译为目标设备支持的底层指令。

编译流程集成点主要在`Circuit.run`方法中：

```python
def run(
    self,
    *,
    provider: Optional[str] = None,
    device: Optional[str] = None,
    shots: int = 1024,
    wait_async_result: Optional[bool] = False,
    **opts: Any,
) -> Any:
```

当电路没有预编译的源代码时，`run`方法会调用`compile`方法进行自动编译。对于TyxonQ硬件，系统会使用Qiskit编译器将电路转换为QASM2格式。编译过程会考虑目标设备的原生门集和连接性约束，生成优化的量子电路。

**更新** 现在针对不同提供商的编译需求进行了优化，确保生成的代码能够正确运行在各种硬件平台上。

`auto_compile`机制确保了高级量子算法可以无缝地在不同硬件平台上执行，而无需用户手动处理编译细节。

**章节来源**
- [circuit.py](file://src/tyxonq/core/ir/circuit.py)

## IBM驱动现状

目前，IBM驱动的提交功能尚未完全实现：

```python
def submit_task(
    device: str,
    token: Optional[str] = None,
    *,
    circuit: Optional[Union[Any, Sequence[Any]]] = None,
    source: Optional[Union[str, Sequence[str]]] = None,
    shots: Union[int, Sequence[int]] = 1024,
    **opts: Any,
) -> List[Any]:
    raise NotImplementedError("IBM driver is a skeleton; submission not wired yet")
```

该实现只是一个骨架，调用时会抛出`NotImplementedError`异常。这表明IBM量子设备的集成仍在进行中，尚未完成任务提交功能的开发。用户在尝试使用IBM设备时需要选择其他可用的硬件提供商或使用本地模拟器。

**章节来源**
- [driver.py](file://src/tyxonq/devices/hardware/ibm/driver.py)